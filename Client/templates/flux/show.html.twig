{% extends 'base.html.twig' %} {% block title %}Flux{% endblock %} {% block body %}
<div class="article-container">
    <h1>Flux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ flux.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ flux.name }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ flux.link }}</td>
            </tr>
            {#
            <tr>
                <th>Created_At</th>
                <td>{{ flux.createdAt ? flux.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Update_At</th>
                <td>{{ flux.updateAt ? flux.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr> #}
        </tbody>
    </table>

    <div class="align-center">
        <button type="button" class="btn btn-primary"><a href="{{ path('flux_index') }}">Voir la liste</a></button>

        <button type="button" class="btn btn-primary" style="margin: 0 20px;"><a href="{{ path('flux_edit', {'id': flux.id}) }}">Modifier</a></button> {{ include('flux/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}